@using Kendo.Mvc.UI
@{
    string wrapId = Guid.NewGuid().ToString("N");
}


@(Html.Kendo().Grid<Data.Entities.Product>()
        .Name(wrapId)
        .DataSource(ds=> ds
            .Ajax()
            .Read(read=> read.Action("Products", "Product"))

    ))
